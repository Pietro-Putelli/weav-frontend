import React, { memo } from "react";
import FullSheetModal from "../FullSheetModal";

const IncreaseVisiblityModal = () => {
  return <FullSheetModal title={"Increase visibility"}></FullSheetModal>;
};

export default memo(IncreaseVisiblityModal);
