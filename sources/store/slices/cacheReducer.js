import { createSlice } from "@reduxjs/toolkit";

const cacheReducer = createSlice({
  name: "cache",
  initialState: {},
  reducers: {},
});

export const {} = cacheReducer.actions;

export default cacheReducer.reducer;
