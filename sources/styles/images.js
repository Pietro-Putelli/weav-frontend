const IMAGE_PATH = "../assets/images";

const images = {
  People: require(`${IMAGE_PATH}/people.png`),
  Event: require(`${IMAGE_PATH}/event.png`),
  CustomerService: require(`${IMAGE_PATH}/customer.support.png`),
  CameraBg: require(`${IMAGE_PATH}/camera.png`),
};

export default images;
